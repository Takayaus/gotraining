<div>
    <%= form_with(url: post_replies_path, local: true ) do |f| %>
    <div>
        <%= f.label :content, "回答" %><br>
        <%= f.text_area :content %>
    </div>
    <%= f.submit %>
    <% end %>
</div>